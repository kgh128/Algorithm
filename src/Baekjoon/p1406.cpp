#include <iostream>
#include <stack>
#include <string>

using namespace std;


int main(void) {
    // 0. 입출력 시간 줄이기
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    // 1. 변수 선언
    int M;
    string str;
    char buf;
    Stack<char> left, right;    // 커서를 기준으로 왼쪽, 오른쪽 문자열

    // 2. 입력받기
    cin >> str >> M;

    for (int i = 0; i < str.length)

    // 3. 입력받은 명령 처리
    for (int i = 0; i < M; i++)
}